# 翻译接口密钥模板（请复制为 .env.production 后填入你的真实 key）
#
# 用法：
#   cp .env.production.example .env.production
#   # 然后编辑 .env.production，把下面的值替换为你的 TRANSLATOR_API_KEY
#
# 说明：
#   - .env.production 已被 .gitignore 排除，不会提交到 GitLab，可放心写入真实密钥。
#   - .env.production.example 会留存在仓库里，供他人 clone 后照模板生成自己的配置。
#   - 此处留空（TRANSLATOR_API_KEY=）时，app 会用源码里的兜底 key 先跑起来，
#     翻译立即可用；填入你自己的 key 后则优先使用你的值。
TRANSLATOR_API_KEY=
